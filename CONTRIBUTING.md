# 🤝 H-CodeLab FE 협업 가이드

H-CodeLab 프론트엔드 팀이 함께 지키는 규칙입니다.

---

## 📌 목차

1. [한눈에 보는 작업 흐름](#1-한눈에-보는-작업-흐름)
2. [브랜치 규칙](#2-브랜치-규칙)
3. [커밋 메시지 규칙](#3-커밋-메시지-규칙)
4. [이슈 규칙](#4-이슈-규칙)
5. [PR 규칙](#5-pr-규칙)
6. [코드 스타일](#6-코드-스타일)
7. [환경변수 & 보안](#7-환경변수--보안)
8. [배포](#8-배포)

---

## 1. 한눈에 보는 작업 흐름

```
이슈 생성  →  브랜치 생성  →  작업 & 커밋  →  PR 생성  →  코드 리뷰  →  master 머지  →  deploy 반영
  (#12)     feat/#12-login    feat: ... (#12)                 승인 1명↑      Squash merge
```

1. 작업 전 **이슈를 먼저 만듭니다.**
2. 최신 `master`에서 **작업 브랜치를 생성**합니다.
3. 작업 후 규칙에 맞춰 **커밋**하고 원격에 push 합니다.
4. `master`를 대상으로 **PR을 생성**합니다.
5. **리뷰 승인** 후 머지하고, 작업 브랜치는 삭제합니다.

---

## 2. 브랜치 규칙

### 브랜치 종류

| 브랜치 | 역할 | 직접 push |
|---|---|:---:|
| `master` | 개발 통합 브랜치. 항상 실행 가능한 상태를 유지 | ❌ |
| `deploy` | **운영 배포 브랜치.** push 시 자동 배포됨 | ❌ |
| `타입/#이슈번호-설명` | 개인 작업 브랜치 | ✅ |

### 작업 브랜치 이름

```
<타입>/#<이슈번호>-<간단한-설명>
```

### 브랜치 타입

| 타입 | 언제 쓰나요? | 예시 |
|---|---|---|
| `feat` | 새로운 기능 개발 | `feat/#12-login-page` |
| `fix` | 버그 수정 | `fix/#15-submit-button` |
| `design` | CSS, 레이아웃 등 UI 변경 | `design/#18-tutor-layout` |
| `refactor` | 기능 변화 없는 코드 구조 개선 | `refactor/#20-tutor-page` |
| `docs` | 문서 추가/수정 | `docs/#25-readme` |
| `chore` | 빌드, 패키지, 설정 등 기타 작업 | `chore/#30-eslint-config` |

- 설명은 **영어 소문자 + 하이픈(-)** 으로 짧게 작성합니다.
- 위 6개에 딱 맞지 않는 작업(테스트 등)은 **가장 가까운 타입**을 사용합니다.

### 브랜치 만들기

```bash
git checkout master
git pull origin master
git checkout -b feat/#12-login-page
```

---

## 3. 커밋 메시지 규칙

### 형식

```
<타입>: <제목> 

<본문> (선택)
```

### 예시

```
fix: 과제 목록 새로고침 시 빈 화면 표시 오류 수정 
```

### 커밋 타입

| 타입 | 언제 쓰나요? |
|---|---|
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정 |
| `design` | CSS, 레이아웃 등 UI 디자인 변경 |
| `refactor` | 기능 변화 없는 코드 구조 개선 |
| `docs` | 문서 추가/수정 (README, CONTRIBUTING 등) |
| `test` | 테스트 코드 추가/수정 |
| `chore` | 빌드, 패키지, 설정 파일 등 기타 작업 |

> 이슈 템플릿의 타입(`FEAT/FIX/DOCS/DESIGN/REFACTOR/CHORE`)과 동일하게 맞춰 사용합니다.

### 작성 규칙

- 제목은 **한글**로, **50자 이내**로 작성합니다.
- 제목 끝에 **마침표를 붙이지 않습니다.**
- "무엇을", "왜" 바꿨는지가 드러나게 작성합니다.
- `수정`, `update`, `ㅇㅇ`처럼 의미 없는 메시지는 쓰지 않습니다.

---

## 4. 이슈 규칙

- 모든 작업은 **이슈를 먼저 생성**한 뒤 시작합니다.
- 이슈 템플릿을 사용하며, 제목은 아래 형식을 따릅니다.

```
[feat] 과제 제출 페이지 구현
[fix] 과제 목록 새로고침 시 빈 화면 표시
```

- **담당자(Assignee)** 를 지정하고, To-do 체크리스트로 진행 상황을 관리합니다.

---

## 5. PR 규칙

### PR 제목

커밋 메시지와 같은 형식을 사용합니다.

```
feat: 과제 제출 페이지 추가 
```

### PR 본문

- PR 템플릿을 채워 작성합니다.
- 본문에 `Closes #12`를 적으면 머지 시 이슈가 **자동으로 닫힙니다.**

### PR 올리기 전 체크리스트

- [ ] 최신 `master`를 반영했나요? (`git pull origin master`)
- [ ] 로컬에서 `npm start`로 정상 실행되나요?
- [ ] `npm run build`가 에러 없이 통과하나요?
- [ ] 콘솔 에러 / 불필요한 `console.log`를 정리했나요?
- [ ] `.env` 등 민감한 파일이 포함되지 않았나요?

### 머지 규칙

- **리뷰어 1명 이상 승인** 후 머지합니다.
- 머지 방식은 **Squash and merge**로 통일합니다.
- 머지는 **PR 작성자**가 직접 하고, 머지 후 **작업 브랜치를 삭제**합니다.(자동)

---

## 6. 코드 스타일

### 네이밍

| 대상 | 규칙 | 예시 |
|---|---|---|
| 컴포넌트, 컴포넌트 파일 | PascalCase | `NoticeManagement.tsx` |
| 변수, 함수 | camelCase | `fetchAssignments` |
| 상수 | UPPER_SNAKE_CASE | `MAX_FILE_SIZE` |
| CSS 클래스 | kebab-case | `.tutor-search-input` |

### CSS

- Tutor 페이지 CSS는 **페이지 컨테이너 클래스로 스코프를 제한**합니다.
  - 예: `.notice-management .tutor-search-input` ✅ / `.tutor-search-input` ❌

### 기타

- 사용하지 않는 import, 변수, 주석 처리된 코드는 커밋 전에 정리합니다.
- API 호출 주소는 코드에 직접 쓰지 않고 **환경변수**를 사용합니다.

---

## 7. 환경변수 & 보안

- `.env` 파일은 **절대 커밋하지 않습니다.** (`.gitignore`에 등록되어 있음)
- 실제 값은 **팀 메신저 등 저장소 밖의 안전한 채널**로 공유합니다.
- 새로운 환경변수를 추가했다면 팀원에게 **꼭 공유**해 주세요.
- 배포에 필요한 값은 **GitHub Secrets**에서 관리합니다.
- 비밀번호, 토큰, API 키를 코드에 직접 쓰지 않습니다.

---

## 8. 배포

- `deploy` 브랜치에 push 되면 GitHub Actions(`deploy-frontend.yml`)가 **자동으로 배포**합니다.
- 따라서 `deploy` 브랜치에는 **직접 push하지 않고**, 팀원과 합의 후 `master` → `deploy`로 반영합니다.
- 배포 후에는 운영 화면에서 **주요 기능이 정상 동작하는지 확인**합니다.
